runtime: custom
env: flex
service: ${int.test.service}

manual_scaling:
  instances: 1

liveness_check:
  path: "/liveness_check"
  check_interval_sec: 30
  timeout_sec: 4
  failure_threshold: 2
  success_threshold: 2
